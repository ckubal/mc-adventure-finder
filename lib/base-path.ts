/**
 * Base path when the app is served under a path (e.g. /metime on weirdlittleideas.com).
 * Set NEXT_PUBLIC_BASE_PATH in env; default /metime for this project.
 */
export const basePath = process.env.NEXT_PUBLIC_BASE_PATH ?? "/metime";
